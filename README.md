# 📚 study tools · 大学四年个人学习管理

以**学习方向**为单位（如「嵌入式」），把方向拆成 **阶段 → 任务**，一步步勾选推进；
每天用 **TIL 速记** 记录学了什么、踩了什么坑；用 **热力图 / 周时长 / 周报回顾** 看见自己的坚持。

> 单机本地 Web 应用：数据全部存在本机 SQLite，无账号、无云端、完全离线可用。

## 快速开始

```bash
# 1. 安装依赖（仅 Flask 一个）
pip install -r requirements.txt

# 2. 启动（或直接双击 run.bat）
python app/main.py

# 3. 浏览器打开
#    http://127.0.0.1:5000
```

首次使用：首页点 **「载入示例数据（嵌入式）」** 可以一键看到完整效果（含 4 个阶段、17 个任务、约 60 天学习记录），
之后删掉示例方向即可开始记录自己的。

## 功能一览（MVP v1.0）

| 模块 | 功能 |
|---|---|
| 方向管理 | 创建 / 编辑 / 删除学习方向，总览进度、累计时长、连续天数 |
| 路线图与进度 | 方向 → 阶段 → 任务三级结构；状态：未开始 → 进行中 → 已完成（可跳过）；三级进度条实时联动 |
| 学习日志 TIL | 按日期记录时长与心得，时间线回看，支持编辑删除 |
| 统计与回顾 | 近一年学习热力图、近 12 周时长条形图、阶段进度、周报（每日时间线 + 完成任务清单） |
| 数据 | SQLite 单文件存储（`app/data/study.db`），备份 = 复制文件 |

## 目录结构

```
study tools/
├── app/
│   ├── main.py          # Flask 入口 + 全部 API
│   ├── database.py      # SQLite 连接与建表
│   ├── demo_data.py     # 「嵌入式」示例数据
│   ├── static/          # 前端（Vue 3 + ECharts，已本地化，免构建）
│   └── data/            # study.db（运行时生成）
├── docs/                # 标准化流程文档
│   ├── 01-竞品调研报告.md
│   ├── 02-需求规格说明书.md
│   └── 03-概要设计.md
├── requirements.txt
├── run.bat / run.sh
└── README.md
```

## 技术栈

- **后端**：Python 3 + Flask 3 + SQLite（标准库 sqlite3）
- **前端**：Vue 3 + ECharts 5（vendor 本地化，无需 Node 构建）
- **启动**：一条命令 / 双击 run.bat

## 后续路线（V2 候选）

- 复习卡片（FSRS 间隔重复）· 番茄钟 · 任务拖拽排序
- 数据导出/导入（JSON / CSV / Markdown）
- 移动端适配 · AI 生成学习路线图
- 引入 Vite 构建链与组件拆分（当项目长大时）

## 开发流程说明

本项目按标准化软件流程推进，各阶段文档见 `docs/`：
竞品调研 → 需求规格说明书（SRS）→ 概要设计 → 编码实现 → 测试验证 → 交付维护。
